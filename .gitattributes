# One line ending everywhere. The repo is edited on Windows and run on
# Linux in CI; without this every commit on Windows warns about CRLF.
* text=auto eol=lf
*.png binary
*.ico binary
*.zip binary
*.docx binary
*.pdf binary
# the model files exactly as published (models/<id>/NOTICE.md): no line-ending changes, no diffs
models/** binary
