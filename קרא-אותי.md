# איך זה מגיע אליה

היא לא מריצה קובץ ולא מתקינה תוכנה. **היא לוחצת על קישור.**
כדי שיהיה קישור, מישהו צריך להעלות את ארבעת הקבצים האלה פעם אחת —
וזה אתה, לא היא. עשר דקות, פעם בחיים.

---

## למה בכלל צריך כתובת

דפדפנים מתייחסים לקובץ שנפתח ישירות מהמחשב (`file://`) כמקור אטום:
אין מטמון, אין אחסון, וטעינת מודול דינמית נחסמת ב-CORS. מקובץ מקומי
מודל הזיהוי לא רק שלא נשמר — הוא בכלל לא נטען.

מכתובת `https` הכל עובד: המודל יורד פעם אחת, נשמר לצמיתות, ומשם
והלאה הכלי עובד גם בלי אינטרנט.

---

## שלב א׳ — להעלות (אתה, פעם אחת)

ארבעת הקבצים חייבים לשבת יחד באותה כתובת:

    index.html
    sw.js
    manifest.webmanifest
    icon.svg

### הדרך המהירה — Netlify Drop

1. היכנס ל-`app.netlify.com/drop`
2. גרור את **התיקייה כולה** לתוך העמוד
3. תוך שניות מתקבלת כתובת

הרשמה חינם נדרשת כדי שהכתובת תישמר. אפשר גם לשנות את שם
הכתובת להודעה קריאה יותר בהגדרות האתר.

### הדרך היציבה — GitHub Pages

בלי git ובלי טרמינל, הכל מהדפדפן:

1. `github.com` → הרשמה (חינם)
2. **New repository** → שם, למשל `hashchara` → **Public** → Create
3. בעמוד שנפתח: **uploading an existing file** → גרור את ארבעת
   הקבצים → **Commit changes**
4. **Settings** → **Pages** → Source: `Deploy from a branch`
   → Branch: `main` / `(root)` → **Save**
5. אחרי כדקה תופיע הכתובת:
   `https://<המשתמש-שלך>.github.io/hashchara/`

חייב `https`. בלי זה אין מטמון ואין service worker.

> הכתובת ציבורית, וזה בסדר: אין בה שום נתון. הכלי לא שולח מסמכים
> לשומקום ואין לו צד שרת. מה שציבורי הוא הכלי, לא התוכן.

**בדוק בעצמך לפני ששולח לה:** פתח את הכתובת, הדלק "זיהוי חכם",
גרור מסמך, וודא שהמודל יורד ושהתוצאה נראית טוב.

---

## שלב ב׳ — מה לשלוח לה

> היי, הכנתי כלי שמחליף את הפרטים המזהים במסמך לפני שאת מדביקה
> אותו ב-ChatGPT, ומחזיר את השמות האמיתיים לתשובה שהוא נותן.
>
> הקישור: <הכתובת>
>
> בפעם הראשונה:
> 1. פתחי את הקישור בכרום או באדג'
> 2. אם יופיע כפתור "התקן" בשורת הכתובת — לחצי עליו, ויהיה לך
>    סמל על שולחן העבודה כמו כל תוכנה
> 3. גררי מסמך Word פנימה
>
> ההפעלה הראשונה לוקחת כמה דקות כי הוא מוריד את מנוע זיהוי השמות.
> אחרי זה זה מיידי, וזה עובד גם בלי אינטרנט.
>
> המסמך לא נשלח לשום מקום. הכל קורה אצלך במחשב.

---

## מה היא רואה

**פעם ראשונה:** פותחת קישור → "התקן" → סמל על שולחן העבודה.
המודל יורד ברקע עם סרגל התקדמות.

**מכאן והלאה:** לוחצת על הסמל. נפתח חלון משלו, בלי סרגלי דפדפן.
גוררת מסמך. בלי הורדה, בלי אינטרנט, בלי דפדפן פתוח.

אם היא לא תלחץ "התקן" — לא נורא. הקישור עובד בדיוק אותו דבר,
והמודל נשמר בכל מקרה. ההתקנה רק נותנת סמל וחלון נקי.

---

## מה עובר ברשת

בהורדה הראשונה בלבד: הכלי עצמו, ספריית ההרצה מ-jsDelivr, ומשקלי
המודל מ-Hugging Face.

**המסמך לא עובר לשום מקום, אף פעם.** הקריאה, הזיהוי, ההחלפה
והאימות קורים בדפדפן. אין צד שרת שמריץ חישוב, ולכן אין לאן
שהתוכן יגיע.

---

## אם אין כתובת

הקובץ הבודד `השחרת-מסמכים.html` עובד בלחיצה כפולה, בלי שום הכנה.
אין מודל — ויש כל השאר: הזיהוי ההיוריסטי, סורק שיבושי התמלול,
כיסוי כל צורות השם, האימות וההחזרה. הכלי אומר לה את זה מפורשות
במסך הראשון, במקום להיכשל בשקט.
